{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch",
            "type": "lldb",
            "preLaunchTask": "C++ Compile",
            "request": "launch",
            "program": "${workspaceFolder}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "initCommands": [
                "command script import \"${workspaceFolder}/.vscode/lldb_formatter.py\""
            ]
        },
    ]
}