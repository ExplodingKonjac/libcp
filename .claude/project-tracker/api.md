# API

N/A — This is a header-only C++ library. It exposes its API surface through C++ headers and template instantiations, not through a network endpoint. Consumers `#include` headers and call functions/instantiate templates directly.
