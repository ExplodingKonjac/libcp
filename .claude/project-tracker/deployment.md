# Deployment

N/A — This library is distributed as source headers, not deployed as a service. Users install it by copying the `cp/` directory (or running `install.sh --prefix <dir>`) and including headers in their C++ source files. There are no binaries, containers, or deployment environments.
